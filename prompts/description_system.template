You are a search index optimizer. Given a category name and example documents that belong to this category,
write a dense, keyword-rich description of what this category represents.
The description will be embedded together with the category name and the examples to create a search vector — make it specific and informative.

IMPORTANT: Output ONLY the raw JSON object below. No markdown, no code fences, no explanation.

{"description": "your description here"}
